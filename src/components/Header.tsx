import GoBackBtn from '@/components/GoBackBtn';

const Header = () => {
  return (
    <header>
      <nav>
        <GoBackBtn />
        Header
      </nav>
    </header>
  );
};

export default Header;
